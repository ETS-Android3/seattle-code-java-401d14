package datastructures.tree;

public class BinaryTree
{
  Node root;

  public Integer[] preOrderTraversal()
  {
    Integer[] outputValues = null;
    // Should return something like "2 -5 20"
    // Can use an ArrayList internally
    // ArrayList.toArray() returns Integer[]
    // outputValues = preOrder(root)
    // TODO: actually implement me
    return outputValues;
  }

  public Integer[] inOrderTraversal()
  {
    Integer[] outputValues = null;
    // outputValues = inOrder(root)
    // TODO: actually implement me
    return outputValues;
  }

  public Integer[] postOrderTraversal()
  {
    Integer[] outputValues = null;
    // outputValues = postOrder(root)
    return outputValues;
  }
}
